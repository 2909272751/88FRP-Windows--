# 88FRP Windows 原生客户端 v1.0.0

这是首个 Windows 原生桌面发布版。

## 下载

请下载本 Release 附件：

```text
88FRP-Windows.zip
```

解压后双击：

```text
88FRP.exe
```

## 新增功能

- 原生 Windows 桌面客户端
- 托盘后台运行
- 首次启动自启动引导
- 注册表启动项 + 计划任务双通道自启动
- 单实例保护，避免重复后台进程
- 隐藏后台核心自动启动
- 远程配置同步
- 隧道选择功能
- 只运行勾选的隧道
- 同步后记住隧道选择，新隧道默认关闭
- 配置编辑、日志查看、启动/停止/重启实例
- 高 DPI 适配
- 专属 Logo 和 Windows 图标

## 使用说明

建议将压缩包解压到固定目录，例如：

```text
D:\Apps\88FRP\
```

不要放在临时下载目录，因为自启动会记录 `88FRP.exe` 所在路径。

## 注意事项

- 不要单独运行 `88frp-web.exe`，它是后台核心。
- 如果同步配置提示 500，请检查密钥是否正确，或稍后重试 88FRP 平台接口。
- 当前版本是登录后自启动的托盘程序，不是系统级 Windows Service。

## 验证

- `npm test` 通过
- Windows 原生客户端可启动后台核心
- `88frp-web.exe` 健康检查正常
