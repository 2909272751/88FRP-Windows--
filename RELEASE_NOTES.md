# 88FRP Windows 3.0.0

本次覆盖更新重点提升长期后台运行时的稳定性。安装时可直接覆盖旧版，实例、账号、隧道选择、备注名称、访问中心和本机端口设置都会保留。

## 稳定性更新

- 主隧道和访问中心新增持续守护；网络抖动或 FRPC 异常退出后会自动恢复。
- 修正 Windows 高权限进程被误判为退出的问题，避免重复启动 FRPC。
- 升级时会识别并安全接管旧版运行记录，同时防止 PID 被其他程序复用时误停进程。
- 桌面端重启后台核心前会先安全交接实例，恢复后继续运行原来已开启的隧道。
- 访问中心配置启用 `loginFailExit = false`，临时 DNS、TCP 或 EOF 错误不会导致永久离线。
- 自动同步最低间隔调整为 5 分钟；遇到超时、HTTP 429 或服务端错误时自动退避并减少重复报错。
- 运行日志按约 5 MB 自动轮换，保留最近两份历史日志；查看日志时只读取末尾内容。
- 卸载会先安全停止桌面端、后台核心和受管隧道，再清理注册表自启动项与计划任务，避免残留后台启动。
- 卸载默认删除本机实例、日志、账号和访问中心数据；选择“保留数据”后才会保留，方便迁移或重新安装。

## 主要功能

- 原生 Windows 桌面端、托盘后台运行和开机自启动引导。
- 只运行已选择的隧道；同步后保留选择，新隧道默认关闭。
- 88FRP 账号连接、自动登录、隧道备注同步和名称分组。
- 响应式 Web 控制台以及独立只读访问中心。
- 控制台安全登录、访问链接、服务器测速和本机服务端口管理。

## 下载

- `88FRP-Windows-Setup-3.0.0.exe`：推荐，双击安装或直接覆盖升级。
- `88FRP-Windows.zip`：便携版，解压后运行 `88FRP.exe`。

## SHA-256

```text
0A94EDF67328CB5894F3BA3B3E9B8C8E0C81A531DBF74F8A8CCC6FE937643D38  88FRP-Windows-Setup-3.0.0.exe
7ED3208CFF4A329F3749EB05708C9664DF9F5486D7D9D748D82BEF6029CE43FB  88FRP-Windows.zip
```

## 验证

- 自动化测试：56 项通过，1 项因 Windows 平台按预期跳过。
- 已完成自动化回归与 Windows 客户端编译校验；安装包在发布前会重新生成并校验。
- 控制台、主隧道和访问中心均已恢复；后台保持 1 个桌面端、1 个核心和 2 个预期 FRPC 进程。
- 升级前后的账号、实例、隧道选择、备注与访问中心配置已核对保留。

> 当前安装包未使用商业代码签名，Windows 可能显示“未知发布者”。请从本项目 Release 下载并按上述 SHA-256 校验。
